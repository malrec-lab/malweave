# Copy this file to .env and replace the placeholders with local private paths.
# The raw root contains dataset/; the metadata root contains Benign.csv and Ransomware.csv.
MALWEAVE_RANDS_DIR=/path/to/rands/raw
MALWEAVE_RANDS_METADATA_DIR=/path/to/rands/metadata
