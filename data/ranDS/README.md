# RanDS — Local Data

Follows the standard `raw/` / `external/` / `interim/` / `processed/` layout described in [`data/README.md`](../README.md).

For dataset details, access terms, and safety requirements, see the [RanDS dataset card](../../docs/mkdocs/docs/datasets/rands.md). Record the local snapshot date and exported metadata used for this acquisition alongside the run's experiment configuration, per the dataset card's guidance.
